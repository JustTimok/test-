1.что такоу fetch. для чего он нужен и как он работвет

***обертка https. метод позволяющий получать ресурсы по сети асинхронно.






2.что такое promis. для чего он нужен и как он работвет

***это объект который содержит в начале pending, потом  fulfilled либо rejected 





3.когтекст это.

***Контекст JS - это значение переменной this внутри функции. 
Область видимости определяет доступ к переменным при вызове функции и является уникальной для каждого вызова
 




4.стрелочная функция это
***это метод сокращения функции для того что бы оно было удобным, коротким, читабельным



5.async await
***ассинхронный код служит для работы с промисами. всегда возвращает promise
Async await заставляет код, который работает асинхронно, выглядеть, как синхронный код.

async function getData() {
  const req = await fetch('https://jsonplaceholder.typicode.com/todos/1');

  const data = await req.json();
  console.log(data, '<==');
}

